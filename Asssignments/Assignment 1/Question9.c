// print Area of rectangle
#include<stdio.h>

int main()
{
    int a,b;;
    printf("Enter the length of the rectangle:");
    scanf("%d",&a);
    printf("Enter the breadth of the rectangle:");
    scanf("%d",&b);
    printf("The Area of the rectangle is %d",a*b);
    return 0;
}

//we can use float also